export default function MarketplaceDetailPage({ params }: { params: { id: string } }) {
  return (
    <div>
      <h1>Detalle de Publicación</h1>
      {/* Detalle y contacto vía WhatsApp */}
    </div>
  );
}
